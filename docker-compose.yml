services:
  main:
    build: .
    volumes:
      - ${PWD}:/home
      - ${PWD}/tests/cps.json:/var/rinha/source.rinha.json
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2048M